import './about.css';

const About = () => {
    return (
        <section className="about">
            <h2>
                Project that gets user data from api, displays it as cards and contains functionality as add to favorite, view more data . Also saves favorite
                users in localstorage.
            </h2>
        </section>
    );
};

export default About;
